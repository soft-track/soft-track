/** An attached image in rendered markdown, while it loads and if it cannot. */
export const image = {
  loading: 'Loading image',
  loadingNamed: 'Loading {{name}}',
  failed: 'This image could not be loaded',
} as const
