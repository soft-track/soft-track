// Cycles (#106): the banner above the board, the list in the sidebar, and the new cycle dialog.
import { schedule } from '@/i18n/en/cycles/schedule'
import { banner } from '@/i18n/en/cycles/banner'
import { list } from '@/i18n/en/cycles/list'
import { newCycle } from '@/i18n/en/cycles/newCycle'

export const cycles = {
  schedule,
  banner,
  list,
  newCycle,
} as const
