// Keyboard (#106): the command palette, its commands, and the shortcuts cheatsheet.
import { commands } from '@/i18n/en/keyboard/commands'
import { palette } from '@/i18n/en/keyboard/palette'
import { shortcuts } from '@/i18n/en/keyboard/shortcuts'

export const keyboard = {
  commands,
  palette,
  shortcuts,
} as const
