// Markdown (#106): the renderer and the editor.
import { editor } from '@/i18n/en/markdown/editor'
import { render } from '@/i18n/en/markdown/render'

export const markdown = {
  editor,
  render,
} as const
