# Test and lint tooling. Deliberately separate from requirements.txt so the
# production image (see Dockerfile) does not ship pytest and black.
-r requirements.txt

black==26.5.1
httpx==0.28.1
pytest==9.1.1
pytest-cov==7.1.0
